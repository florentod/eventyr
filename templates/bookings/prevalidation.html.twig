{% extends 'base.html.twig' %}

{% block title %}Bookings{% endblock %}

{% block body %}
    <h1>Bookings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ datesPrices.id }}</td>
            </tr>
            <tr>
                <th>Comment_content</th>
                <td>{{ datesPrices.offer.offerName }}</td>
            </tr>
            <tr>
                <th>offers price</th>
                <td>{{ datesPrices.price }}</td>
            </tr>
             <tr>
                <th>Offer_type</th>
                <td>{{ datesPrices.offer.offerType }}</td>
            </tr>
            <tr>
                <th>Offer_hosting</th>
                <td>{{ datesPrices.offer.offerHosting }}</td>
            </tr>
            <tr>
                <th>Offer_difficulty</th>
                <td>{{ datesPrices.offer.offerDifficulty }}</td>
            </tr>
            <tr>
                <th>Offer_description</th>
                <td>{{ datesPrices.offer.offerDescription }}</td>
            </tr>
            <tr>
                <th>Offer_map_photo</th>
                <td>{{ datesPrices.offer.offerMapPhoto }}</td>
            </tr>
            <tr>
                <th>Offer_start_photo</th>
                <td>{{ datesPrices.offer.offerStartPhoto }}</td>
            </tr>
            <tr>
                <th>qui a réservé</th>
                <td>{{ app.user.id }}</td>
            </tr>
        </tbody>
    </table>

   {#  <a href="{{ path('bookings_index') }}">back to list</a>

    <a href="{{ path('bookings_edit', {'id': booking.id}) }}">edit</a> #}

   {#  {{ include('bookings/_delete_form.html.twig') }} #}
   <a href="{{ path('bookings_validation', {'id': datesPrices.id}) }}">Create new</a>
   <a href="{{ path('home') }}">retour a l'accueil</a>
{% endblock %}
